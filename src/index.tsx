import styled from "styled-components";
import { FontSize } from "./resources/constants";

export const H1 = styled.body`
  font: ${FontSize.Small} "Century Gothic", Futura, sans-serif;
  margin: 20px;
`;

  